#include "gtest.h"
#include "../tables/sorted_array_table.h"

TEST(Sorted_array_table, can_create_table)
{
	ADD_FAILURE();
}

TEST(Sorted_array_table, can_insert_elem)
{
	ADD_FAILURE();
}

TEST(Sorted_array_table, throws_when_insert_if_table_is_full)
{
	ADD_FAILURE();
}

TEST(Sorted_array_table, can_delete_elem)
{
	ADD_FAILURE();
}

TEST(Sorted_array_table, throws_when_delete_if_table_is_empty)
{
	ADD_FAILURE();
}

TEST(Sorted_array_table, can_find_elem)
{
	ADD_FAILURE();
}

TEST(Sorted_array_table, throws_when_cant_find_elem)
{
	ADD_FAILURE();
}

TEST(Sorted_array_table, stay_sorted_after_insert)
{
	ADD_FAILURE();
}